#!/usr/bin/env python3
"""Erweiterte PowerGateway-WebGUI für Zähler, MQTT und Home Assistant."""
from __future__ import annotations

import json
import socket
from copy import deepcopy
from typing import Any

import webapp as legacy
from flask import Response, jsonify, request, session
from runtime_config import RUNTIME_CONFIG_PATH, load_runtime, merged_config

app = legacy.app

PAGE = r'''<!doctype html><html lang="de"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>PowerGateway</title><style>__STYLE__</style></head><body>
<header class="head"><div><h1>PowerGateway</h1><small id="subtitle">Status wird geladen …</small></div><div><strong id="health">Wird geprüft</strong> · <a class="button secondary" href="/logout">Abmelden</a></div></header>
<nav class="nav"><button class="active" onclick="showTab('dashboard',this)">Dashboard</button><button onclick="showTab('meter',this)">Stromzähler</button><button onclick="showTab('mqtt',this)">MQTT & Home Assistant</button><button onclick="showTab('network',this)">Netzwerk</button><button onclick="showTab('users',this)">Benutzer</button><button onclick="showTab('diagnostics',this)">Diagnose</button></nav>
<main class="wrap"><div id="notice" class="notice"></div>
<section id="dashboard" class="tab active"><div id="cards" class="grid"></div><div class="section card"><h2>Aktuelle Zählerwerte</h2><table><thead><tr><th>Messwert</th><th>Wert</th><th>Einheit</th><th>OBIS</th></tr></thead><tbody id="measurements"></tbody></table></div></section>
<section id="meter" class="tab"><div class="toolbar"><div><h2>Stromzähler</h2><div class="muted">Genau eine Datenquelle ist gleichzeitig aktiv.</div></div><button onclick="saveApplication()">Speichern und anwenden</button></div><div class="two"><div class="card"><div class="form-grid"><div class="field full"><label>Datenquelle</label><select id="meterSource" onchange="toggleMeterSource()"><option value="usb_sml">USB-SML-Lesekopf</option><option value="tasmota_mqtt">Tasmota MQTT</option><option value="generic_mqtt">Generic MQTT</option><option value="simulation">Simulation</option></select></div><div id="usbFields" class="field full"><div class="form-grid"><div class="field"><label>Gerät</label><input id="meterDevice" placeholder="auto oder /dev/serial/by-id/..."></div><div class="field"><label>Baudrate</label><input id="meterBaudrate" type="number" min="300"></div></div></div><div id="simulationFields" class="field full"><div class="form-grid"><div class="field"><label>Simulationsprofil</label><select id="simulationProfile"><option value="generic">Generisch</option><option value="household">Haushalt</option><option value="solar">PV mit Einspeisung</option></select></div><div class="field"><label>Intervall in Sekunden</label><input id="simulationInterval" type="number" min="1" step="0.5"></div><div class="field"><label>Zufalls-Seed</label><input id="simulationSeed" type="number"></div></div></div><div id="mqttSourceFields" class="field full"><div class="form-grid"><div class="field full"><label>Eingangs-Topic</label><input id="sourceTopic" placeholder="tele/Stromzaehler/SENSOR"></div><div class="field"><label>Pfad aktuelle Leistung</label><input id="powerPath" placeholder="Home.Power_curr"></div><div class="field"><label>Pfad Energiebezug</label><input id="importPath" placeholder="Home.total_in"></div><div class="field"><label>Pfad Einspeisung</label><input id="exportPath" placeholder="Home.total_out"></div></div></div></div></div><div class="card"><h2>Status</h2><div id="meterStatus"></div><p class="muted">Nach dem Speichern wird der PowerGateway-Dienst automatisch neu gestartet.</p></div></div></section>
<section id="mqtt" class="tab"><div class="toolbar"><div><h2>MQTT und Home Assistant</h2><div class="muted">Broker-Zugang und automatische Geräteerkennung.</div></div><button onclick="saveApplication()">Speichern und anwenden</button></div><div class="two"><div class="card"><h2>MQTT-Broker</h2><div class="form-grid"><label class="check full"><input id="mqttEnabled" type="checkbox"> MQTT-Ausgabe aktivieren</label><div class="field"><label>Broker</label><input id="mqttHost" placeholder="192.168.178.50"></div><div class="field"><label>Port</label><input id="mqttPort" type="number" min="1" max="65535"></div><div class="field"><label>Benutzername</label><input id="mqttUsername"></div><div class="field"><label>Passwort</label><input id="mqttPassword" type="password" placeholder="Leer = beibehalten"></div><div class="field full"><label>Topic-Präfix</label><input id="mqttTopicPrefix" placeholder="powergateway/powergateway-01"></div><label class="check"><input id="mqttTls" type="checkbox"> TLS verwenden</label><div class="field"><label>CA-Datei</label><input id="mqttCaFile" placeholder="/etc/ssl/certs/ca-certificates.crt"></div><div class="field full"><button class="secondary" onclick="testMqtt()">Broker-Verbindung testen</button></div></div></div><div class="card"><h2>Home Assistant</h2><div class="form-grid"><label class="check full"><input id="haDiscovery" type="checkbox"> MQTT Discovery aktivieren</label><div class="field full"><label>Discovery-Präfix</label><input id="haPrefix" placeholder="homeassistant"></div><div class="field full"><label>Gerätename</label><input id="gatewayName"></div></div><p class="muted">Discovery veröffentlicht Sensoren für erkannte OBIS-Werte automatisch.</p><div id="mqttStatus" class="section"></div></div></div></section>
<section id="network" class="tab"><div class="toolbar"><div><h2>Netzwerkverwaltung</h2><div class="muted">LAN, WLAN, LTE und Setup-Hotspot</div></div><button onclick="saveNetwork()">Speichern und anwenden</button></div><div id="networkCards" class="grid"></div><div class="section two"><div class="card"><h2>Priorität und LAN</h2><div class="form-grid"><div class="field"><label>1. Verbindung</label><select id="prio1"><option value="lan">LAN</option><option value="wifi">WLAN</option><option value="lte">LTE</option></select></div><div class="field"><label>2. Verbindung</label><select id="prio2"><option value="wifi">WLAN</option><option value="lan">LAN</option><option value="lte">LTE</option></select></div><div class="field"><label>3. Verbindung</label><select id="prio3"><option value="lte">LTE</option><option value="wifi">WLAN</option><option value="lan">LAN</option></select></div><label class="check"><input id="lanEnabled" type="checkbox"> LAN aktiv</label><label class="check"><input id="lanDhcp" type="checkbox"> DHCP</label><div class="field"><label>Schnittstelle</label><input id="lanInterface"></div><div class="field"><label>Profilname</label><input id="lanConnection"></div><div class="field"><label>IP/CIDR</label><input id="lanAddress"></div><div class="field"><label>Gateway</label><input id="lanGateway"></div><div class="field full"><label>DNS (Komma)</label><input id="lanDns"></div></div></div><div class="card"><div class="toolbar"><h2>WLAN</h2><button class="secondary" onclick="scanWifi()">Suchen</button></div><div class="form-grid"><label class="check full"><input id="wifiEnabled" type="checkbox"> WLAN aktiv</label><div class="field"><label>Schnittstelle</label><input id="wifiInterface"></div><div class="field"><label>Profilname</label><input id="wifiConnection"></div><div class="field full"><label>SSID</label><input id="wifiSsid"></div><div class="field full"><label>Passwort</label><input id="wifiPassword" type="password" placeholder="Leer = beibehalten"></div></div><div id="wifiList" class="section muted">Noch keine Suche.</div></div></div><div class="section two"><div class="card"><h2>LTE</h2><div class="form-grid"><label class="check full"><input id="lteEnabled" type="checkbox"> LTE aktiv</label><div class="field"><label>Profilname</label><input id="lteConnection"></div><div class="field"><label>APN</label><input id="lteApn"></div><div class="field"><label>Benutzername</label><input id="lteUsername"></div><div class="field"><label>Passwort</label><input id="ltePassword" type="password"></div><div class="field"><label>SIM-PIN</label><input id="ltePin" type="password"></div></div></div><div class="card"><h2>Setup-Hotspot</h2><div class="form-grid"><div class="field"><label>Betriebsart</label><select id="hotspotMode"><option value="auto">Automatisch</option><option value="always">Immer an</option><option value="off">Aus</option></select></div><div class="field"><label>Schnittstelle</label><input id="hotspotInterface"></div><div class="field"><label>SSID</label><input id="hotspotSsid"></div><div class="field"><label>Passwort</label><input id="hotspotPassword" type="password" placeholder="Leer = beibehalten"></div><div class="field"><label>IP/CIDR</label><input id="hotspotAddress"></div><label class="check"><input id="hotspotOffline" type="checkbox"> Ohne Internet starten</label></div></div></div></section>
<section id="users" class="tab"><div class="two"><div class="card"><h2>Benutzer</h2><div id="userList"></div></div><div class="card"><h2>Eigenes Passwort ändern</h2><div class="form-grid"><div class="field full"><label>Aktuelles Passwort</label><input id="oldPassword" type="password"></div><div class="field"><label>Neues Passwort</label><input id="newPassword" type="password"></div><div class="field"><label>Wiederholen</label><input id="newPassword2" type="password"></div><div class="field full"><button onclick="changePassword()">Passwort ändern</button></div></div></div></div></section>
<section id="diagnostics" class="tab"><div class="toolbar"><h2>Systemdiagnose</h2><button onclick="loadDiagnostics()">Diagnose starten</button></div><pre id="diagnosticOutput">Die Diagnose wird nur auf Anforderung ausgeführt.</pre></section></main>
<script>
const $=id=>document.getElementById(id),esc=v=>String(v??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function showTab(id,b){document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.nav button').forEach(x=>x.classList.remove('active'));$(id).classList.add('active');b.classList.add('active');if(id==='network')loadNetwork();if(id==='users')loadUsers();if(id==='meter'||id==='mqtt')loadApplication()}
function card(l,v,s=''){return `<div class="card"><div class="label">${esc(l)}</div><div class="value ${s}">${esc(v)}</div></div>`}function notice(t,o=true){const n=$('notice');n.textContent=t;n.className='notice '+(o?'ok':'bad');setTimeout(()=>n.className='notice',7000)}function val(i,v=''){$(i).value=v??''}function chk(i,v){$(i).checked=!!v}
async function api(url,opt={}){const r=await fetch(url,{cache:'no-store',...opt});if(r.status===401){location='/login';throw new Error('Sitzung abgelaufen')}const d=await r.json();if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);return d}
async function refresh(){try{const d=await api('/_internal/overview'),s=d.status||{},v=d.values||{},n=d.network||{};$('subtitle').textContent=`Version ${d.version} · ${d.gateway_name}`;$('health').textContent=s.meter_connected?'Betriebsbereit':'Prüfung erforderlich';$('cards').innerHTML=[card('Stromzähler',s.meter_connected?'Verbunden':'Getrennt',s.meter_connected?'ok':'bad'),card('Datenquelle',v.source||'unbekannt'),card('Leistung',v.power_total!==undefined?`${v.power_total} W`:'Noch kein Wert'),card('MQTT',s.mqtt_connected?'Verbunden':'Getrennt',s.mqtt_connected?'ok':'bad'),card('Aktive Verbindung',(n.active||'keine').toUpperCase(),n.online?'ok':'bad'),card('Setup-Hotspot',n.hotspot_active?'Aktiv':'Inaktiv',n.hotspot_active?'warn':'')].join('');const ms=v.measurements||[];$('measurements').innerHTML=ms.length?ms.map(x=>`<tr><td>${esc(x.name||x.key)}</td><td>${esc(x.value)}</td><td>${esc(x.unit||'')}</td><td>${esc(x.obis||'')}</td></tr>`).join(''):'<tr><td colspan="4">Noch keine Messwerte vorhanden.</td></tr>'}catch(e){notice(e.message,false)}}
function toggleMeterSource(){const s=$('meterSource').value;$('usbFields').style.display=s==='usb_sml'?'block':'none';$('simulationFields').style.display=s==='simulation'?'block':'none';$('mqttSourceFields').style.display=['tasmota_mqtt','generic_mqtt'].includes(s)?'block':'none'}
async function loadApplication(){try{const d=await api('/_internal/application'),c=d.config||{},m=c.meter||{},q=c.mqtt||{},s=d.status||{};val('gatewayName',c.gateway?.name||'PowerGateway');val('meterSource',m.source||'usb_sml');val('meterDevice',m.device||'auto');val('meterBaudrate',m.baudrate||9600);val('simulationProfile',m.simulation_profile||'generic');val('simulationInterval',m.simulation_interval||5);val('simulationSeed',m.simulation_seed??'');const t=m.tasmota||{};val('sourceTopic',t.topic||'');val('powerPath',t.power_path||'Home.Power_curr');val('importPath',t.energy_import_path||'Home.total_in');val('exportPath',t.energy_export_path||'Home.total_out');chk('mqttEnabled',q.enabled);val('mqttHost',q.host||'');val('mqttPort',q.port||1883);val('mqttUsername',q.username||'');val('mqttPassword','');val('mqttTopicPrefix',q.topic_prefix||'');chk('mqttTls',q.tls);val('mqttCaFile',q.ca_file||'');chk('haDiscovery',q.homeassistant_discovery!==false);val('haPrefix',q.discovery_prefix||'homeassistant');toggleMeterSource();$('meterStatus').innerHTML=[['Verbindung',s.meter_connected?'Verbunden':'Getrennt'],['Gerät',s.meter_device||'—'],['Telegramme',s.telegram_count||0],['Letzter Fehler',s.last_error||'Keiner']].map(x=>`<div class="status-row"><span>${esc(x[0])}</span><strong>${esc(x[1])}</strong></div>`).join('');$('mqttStatus').innerHTML=[['MQTT',s.mqtt_connected?'Verbunden':'Getrennt'],['Puffer',s.buffered_messages||0]].map(x=>`<div class="status-row"><span>${esc(x[0])}</span><strong>${esc(x[1])}</strong></div>`).join('')}catch(e){notice(e.message,false)}}
async function saveApplication(){const source=$('meterSource').value,body={gateway:{name:$('gatewayName').value.trim()||'PowerGateway'},meter:{source,device:$('meterDevice').value||'auto',baudrate:Number($('meterBaudrate').value||9600),simulation_profile:$('simulationProfile').value,simulation_interval:Number($('simulationInterval').value||5),simulation_seed:$('simulationSeed').value===''?null:Number($('simulationSeed').value),tasmota:{topic:$('sourceTopic').value.trim(),power_path:$('powerPath').value.trim(),energy_import_path:$('importPath').value.trim(),energy_export_path:$('exportPath').value.trim()}},mqtt:{enabled:$('mqttEnabled').checked,host:$('mqttHost').value.trim(),port:Number($('mqttPort').value||1883),username:$('mqttUsername').value.trim(),password:$('mqttPassword').value,topic_prefix:$('mqttTopicPrefix').value.trim(),tls:$('mqttTls').checked,ca_file:$('mqttCaFile').value.trim(),homeassistant_discovery:$('haDiscovery').checked,discovery_prefix:$('haPrefix').value.trim()||'homeassistant'}};try{const d=await api('/_internal/application',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});notice(d.message);setTimeout(()=>{loadApplication();refresh()},2500)}catch(e){notice(e.message,false)}}
async function testMqtt(){try{const d=await api('/_internal/application/mqtt-test',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({host:$('mqttHost').value.trim(),port:Number($('mqttPort').value||1883)})});notice(d.message,true)}catch(e){notice(e.message,false)}}
async function loadNetwork(){try{const d=await api('/_internal/network'),c=d.config||{},s=d.status||{},p=c.priority||['lan','wifi','lte'];['prio1','prio2','prio3'].forEach((id,i)=>val(id,p[i]));const l=c.lan||{},w=c.wifi||{},m=c.lte||{},h=c.hotspot||{};chk('lanEnabled',l.enabled);chk('lanDhcp',l.dhcp!==false);val('lanInterface',l.interface||'auto');val('lanConnection',l.connection||'PowerGateway-LAN');val('lanAddress',l.address);val('lanGateway',l.gateway);val('lanDns',(l.dns||[]).join(', '));chk('wifiEnabled',w.enabled);val('wifiInterface',w.interface||'auto');val('wifiConnection',w.connection||'PowerGateway-WLAN');val('wifiSsid',w.ssid);val('wifiPassword','');chk('lteEnabled',m.enabled);val('lteConnection',m.connection||'PowerGateway-LTE');val('lteApn',m.apn);val('lteUsername',m.username);val('ltePassword','');val('ltePin','');val('hotspotMode',h.mode||'auto');val('hotspotInterface',h.interface||'wlan0');val('hotspotSsid',h.ssid||'PowerGateway-Setup');val('hotspotPassword','');val('hotspotAddress',h.address||'192.168.50.1/24');chk('hotspotOffline',h.offline_fallback!==false);$('networkCards').innerHTML=[card('Aktiv',(s.active||'keine').toUpperCase(),s.online?'ok':'bad'),card('LAN',s.lan?.state||'unbekannt'),card('WLAN',s.wifi?.state||'unbekannt'),card('LTE',s.lte?.state||'unbekannt'),card('Hotspot',s.hotspot_active?'Aktiv':'Inaktiv',s.hotspot_active?'warn':'')].join('')}catch(e){notice(e.message,false)}}
async function scanWifi(){try{$('wifiList').textContent='Suche läuft …';const d=await api('/_internal/network/wifi-scan');$('wifiList').innerHTML=d.networks?.length?d.networks.map(n=>`<div class="status-row"><strong>${esc(n.ssid)}</strong><span>${n.signal}% · ${esc(n.security)}</span><button class="secondary" onclick="val('wifiSsid',${JSON.stringify(n.ssid)})">Auswählen</button></div>`).join(''):'Keine WLANs gefunden.'}catch(e){$('wifiList').textContent=e.message}}
async function saveNetwork(){const priority=[$('prio1').value,$('prio2').value,$('prio3').value];if(new Set(priority).size!==3){notice('Jede Priorität darf nur einmal verwendet werden.',false);return}const body={priority,lan:{enabled:$('lanEnabled').checked,interface:$('lanInterface').value,connection:$('lanConnection').value,dhcp:$('lanDhcp').checked,address:$('lanAddress').value,gateway:$('lanGateway').value,dns:$('lanDns').value.split(',').map(x=>x.trim()).filter(Boolean)},wifi:{enabled:$('wifiEnabled').checked,interface:$('wifiInterface').value,connection:$('wifiConnection').value,ssid:$('wifiSsid').value,password:$('wifiPassword').value},lte:{enabled:$('lteEnabled').checked,connection:$('lteConnection').value,apn:$('lteApn').value,username:$('lteUsername').value,password:$('ltePassword').value,pin:$('ltePin').value},hotspot:{mode:$('hotspotMode').value,interface:$('hotspotInterface').value,connection:'PowerGateway-Setup',ssid:$('hotspotSsid').value,password:$('hotspotPassword').value,address:$('hotspotAddress').value,offline_fallback:$('hotspotOffline').checked}};try{const d=await api('/_internal/network',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});notice(d.message);setTimeout(loadNetwork,2500)}catch(e){notice(e.message,false)}}
async function loadUsers(){try{const d=await api('/_internal/users');$('userList').innerHTML=d.users.map(u=>`<div class="status-row"><strong>${esc(u.username)}</strong><span>${esc(u.role)} · ${u.enabled?'aktiv':'gesperrt'}</span></div>`).join('')}catch(e){notice(e.message,false)}}
async function changePassword(){if($('newPassword').value!==$('newPassword2').value){notice('Die neuen Passwörter stimmen nicht überein.',false);return}try{const d=await api('/_internal/password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({old_password:$('oldPassword').value,new_password:$('newPassword').value})});notice(d.message);['oldPassword','newPassword','newPassword2'].forEach(x=>val(x,''))}catch(e){notice(e.message,false)}}
async function loadDiagnostics(){try{$('diagnosticOutput').textContent='Diagnose läuft …';const d=await api('/_internal/diagnostics');$('diagnosticOutput').textContent=d.output||'Keine Ausgabe'}catch(e){$('diagnosticOutput').textContent=e.message}}
refresh();setInterval(refresh,5000);
</script></body></html>'''.replace('__STYLE__', legacy.STYLE)
legacy.PAGE = PAGE


def _public_config(config: dict[str, Any]) -> dict[str, Any]:
    safe = deepcopy(config)
    if isinstance(safe.get("mqtt"), dict):
        safe["mqtt"].pop("password", None)
    return safe


def _application_config(include_secrets: bool = False) -> dict[str, Any]:
    config = merged_config(legacy.base_config())
    return config if include_secrets else _public_config(config)


@app.get('/_internal/application')
@legacy.login_required
def get_application() -> Response:
    return jsonify({"config": _application_config(False), "status": legacy.read_json(legacy.DATA_DIR / "status.json")})


@app.post('/_internal/application')
@legacy.login_required
def set_application() -> Response:
    supplied = request.get_json(silent=True)
    if not isinstance(supplied, dict):
        return jsonify({"error": "Ungültige Konfiguration"}), 400
    source = str(supplied.get("meter", {}).get("source", ""))
    if source not in {"usb_sml", "tasmota_mqtt", "generic_mqtt", "simulation"}:
        return jsonify({"error": "Ungültige Datenquelle"}), 400
    mqtt = supplied.get("mqtt", {})
    if not isinstance(mqtt, dict):
        return jsonify({"error": "Ungültige MQTT-Konfiguration"}), 400
    if bool(mqtt.get("enabled")) and not str(mqtt.get("host", "")).strip():
        return jsonify({"error": "Für aktives MQTT muss ein Broker angegeben werden."}), 400
    port = int(mqtt.get("port", 1883))
    if port < 1 or port > 65535:
        return jsonify({"error": "Ungültiger MQTT-Port"}), 400
    if source in {"tasmota_mqtt", "generic_mqtt"} and not str(supplied.get("meter", {}).get("tasmota", {}).get("topic", "")).strip():
        return jsonify({"error": "Für die MQTT-Datenquelle muss ein Eingangs-Topic angegeben werden."}), 400
    current = load_runtime()
    old_password = str(current.get("mqtt", {}).get("password", ""))
    if not str(mqtt.get("password", "")) and old_password:
        mqtt["password"] = old_password
    legacy.atomic_json(RUNTIME_CONFIG_PATH, supplied)
    settings = legacy.settings()
    settings["gateway_name"] = str(supplied.get("gateway", {}).get("name", "PowerGateway")).strip() or "PowerGateway"
    settings["setup_complete"] = True
    legacy.atomic_json(legacy.SETTINGS_PATH, settings)
    return jsonify({"ok": True, "message": "Konfiguration gespeichert. Der PowerGateway-Dienst wird automatisch neu gestartet."})


@app.post('/_internal/application/mqtt-test')
@legacy.login_required
def mqtt_test() -> Response:
    supplied = request.get_json(silent=True) or {}
    host = str(supplied.get("host", "")).strip()
    port = int(supplied.get("port", 1883))
    if not host:
        return jsonify({"error": "Broker-Adresse fehlt."}), 400
    try:
        with socket.create_connection((host, port), timeout=5):
            pass
    except OSError as exc:
        return jsonify({"error": f"Broker nicht erreichbar: {exc}"}), 400
    return jsonify({"ok": True, "message": f"TCP-Verbindung zu {host}:{port} erfolgreich."})
